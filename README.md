Application for experiments with technologies + CI/CD configuration.

### Setup
To configure dev's environment you need run `./gradlew setupDevEnv` first to configure git-hooks
and base IDEA-settings.
